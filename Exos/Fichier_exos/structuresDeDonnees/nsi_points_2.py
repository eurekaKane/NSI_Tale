"""Une seconde implémentation d'un point du plan géométrique."""

# un point est 

import math

# quelques opérations

def creer_point(x : float, y : float) -> :# à compléter
    """Renvoie un point créé à partir de ses deux coordonnées."""
    return # à compléter

def distance_origine(point : ) -> float:# à compléter
    """Renvoie la distance à l'origine du point."""
    # à compléter

def distance(point1 : , point2 : ) -> float:# à compléter
    """Renvoie la distance entre deux points."""
    # à compléter

def image_translation(point : , vecteur : ) -> :# à compléter
    """Renvoie l'image du point par la translation de vecteur."""
    # à compléter