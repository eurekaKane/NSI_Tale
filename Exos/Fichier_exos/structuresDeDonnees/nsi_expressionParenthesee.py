# importation de la structure de Pile
from nsi_pile_1 import *

# associations parenthèses ouvrante - fermante par 2 dictionnaires
ouvrante = { ')' : '(', ']' : '[', '}' : '{'}
fermante = {'(' : ')', '[' : ']', '{' : '}'}

# fonction d'analyse
def analyse_expression(expression : str) -> bool:
    """Analyse le bon parenthésage d'expression et renvoie True si
        expression est correctement parenthésée ou False sinon.
    """
    # pour empiler les parenthèses ouvrantes
    # à compléter
    # parcours de l'expression
    # à compléter
    # fin du parcours
    return # à compléter
