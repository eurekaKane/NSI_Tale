"""Ce module implémente une structure d'arbre binaire
    par des tableaux emboîtés.
"""

import queue  # pour gérer une file SimpleQueue

def creer_ab(r = None , g : list = [], d : list = []) -> list:
    """Renvoie un arbre binaire vide par défaut."""
    pass


def est_vide(ab : list) -> bool:
    """Renvoie True si l'arbre binaire passé en argument
        est vide, False sinon.
    """
    # pré-condition
    
    # traitement
    pass


def est_feuille(ab : list) -> bool:
    """Renvoie True si l'arbre passé en argument est une
        feuille, False sinon.
    """
    # pré-condition
    
    # traitement
    pass


def taille_ab(ab : list) -> int:
    """Renvoie la taille de l'arbre binaire passé en argument.
        Cette fonction est récursive.
    """
    # pré-condition
    
    # traitement
    pass


def hauteur_ab(ab : list) -> int:
    """Renvoie la hauteur de l'arbre binaire passé en argument.
        Cette fonction est récursive.
    """
    # pré-condition
    
    # traitement
    pass


def afficher_ab(ab : list) -> str:
    """Renvoie une chaîne de caractères représentant l'arbre
        binaire passé en argument.
        Cette fonction est récursive.
    """
    # pré-condition
    
    # traitement
    pass


def nbFeuilles_ab(ab : list) -> int:
    """Renvoie le nombre de feuilles de l'arbre binaire passé
        en argument.
        Le calcul repose sur un parcours en largeur.
    """
    # pré-condition
    
    # traitement
    pass


def appartient_ab(val, ab : list) -> bool:
    """Renvoie True si la valeur de val est une celle d'un nœud
        de l'arbre binaire ab, renvoie False sinon.
        Cette fonction est récursive.
    """
    # pré-condition
    
    # traitement
    pass