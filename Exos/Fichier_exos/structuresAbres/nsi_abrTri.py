"""Pour utiliser une structure d'arbre binaire de recherche
    afin de trier un tableau de valeurs comparables.
"""

import nsi_abrTableau as strAbr
import random as rd


def _est_trie(tab : list) -> bool:
    """Renvoie True si les valeurs des éléments du tableau
        tab passé en argument sont triées en ordre croissant.
        Renvoie False sinon.
    """
    pass


def _parcoursInfixe(abr : list) -> list:
    """Renvoie un tableau composés des noeuds de l'arbre
        binaire de recherche passé en argument et décrits par
        un parcours infixe de l'arbre.
    """
    result : list = list()  # pour le résultat
    # moteur récursif
    def pInf_rec(i : int) -> None:
        pass
    # appel principal
    pass


def genere_tab_alea(n : int) -> list:
    """Génère un tableau de n nombres entiers aléatoires
        compris entre -1000 et 1000.
    """
    pass


def tri_tab(tab : list) -> list:
    """Trie les valeurs du tableau tab en utilisant
        un arbre binaire de recherche.
        Renvoie un nouveau tableau contenant les éléments de
        tab triés.
    """
    pass